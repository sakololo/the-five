---
description: Sync context with repository logic and external sources
---

1. **Git Sync**
   - `git fetch origin`
   - `git status`

2. **Context Refresh**
   - Read `SESSION_HANDOFF.md` to get latest state.
   - Check `CLAUDE.md` for rule updates.

3. **Status Report**
   - Report current branch and strict adherence status.
